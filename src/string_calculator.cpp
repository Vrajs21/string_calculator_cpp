#include "../include/string_calculator.hpp"
#include <sstream>
#include <string>
#include <vector>

using namespace std;
int StringCalculator::add(const std::string& numbers) {
    
    if (numbers.empty()) {
        return 0;
    }


}