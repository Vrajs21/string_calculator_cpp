package string_calculator;
import java.util.List;

public interface string_calculator {
	
	int add(String numbers);
    void checkNegative(String numbers);
    String[] getTokens(String numbers);
    
}
